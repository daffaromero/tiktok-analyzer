# TikTok Analyzer

Collects TikTok comments, captions, and subtitles by hashtag/search or video ID/URL,
exports to Excel + CSV. Run `TikTok_Analyzer.ipynb` in Jupyter or Google Colab.

Logic lives in `tiktok_analyzer.py` (unit-tested with `pytest`).
